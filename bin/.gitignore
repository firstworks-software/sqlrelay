# sqlrclient-config, sqlrclientwrapper-config and sqlrserver-config are
# generated by configure from the matching .in template (configure.in's
# AC_CONFIG_FILES at lines 1314-1316, which also chmods each 755).  the
# .in templates are hand maintained and stay tracked (#9751)
/sqlrclient-config
/sqlrclientwrapper-config
/sqlrserver-config
