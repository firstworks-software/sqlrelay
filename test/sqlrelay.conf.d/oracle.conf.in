<?xml version="1.0"?>
<instances>

	<instance id="oracle" dbase="oracle">
		<listeners>
			<listener port="9001" socket="/tmp/oracletest.socket"/>
		</listeners>
		<auths>
			<auth module="sqlrclient_userlist">
				<user user="testuser" password="testpassword"/>
			</auth>
		</auths>
		<connections>
                        <connection string="oracle_sid=(DESCRIPTION = (ADDRESS = (PROTOCOL = TCP)(HOST = oracle)(PORT = 1521)) (CONNECT_DATA = (SERVER = DEDICATED) (SERVICE_NAME = ora1)));user=@HOSTNAME@;password=testpassword"/>
		</connections>
	</instance>

	<instance id="oracle12c" dbase="oracle">
		<listeners>
			<listener port="9001" socket="/tmp/oracletest.socket"/>
		</listeners>
		<auths>
			<auth module="sqlrclient_userlist">
				<user user="testuser" password="testpassword"/>
			</auth>
		</auths>
		<connections>
                        <connection string="oracle_sid=(DESCRIPTION = (ADDRESS = (PROTOCOL = TCP)(HOST = oracle12c)(PORT = 1521)) (CONNECT_DATA = (SERVER = DEDICATED) (SERVICE_NAME = ora1)));user=@HOSTNAME@;password=testpassword"/>
		</connections>
	</instance>

	<instance id="oracle11g" dbase="oracle">
		<listeners>
			<listener port="9001" socket="/tmp/oracletest.socket"/>
		</listeners>
		<auths>
			<auth module="sqlrclient_userlist">
				<user user="testuser" password="testpassword"/>
			</auth>
		</auths>
		<connections>
                        <connection string="oracle_sid=(DESCRIPTION = (ADDRESS = (PROTOCOL = TCP)(HOST = oracle11g)(PORT = 1521)) (CONNECT_DATA = (SERVER = DEDICATED) (SERVICE_NAME = ora1)));user=@HOSTNAME@;password=testpassword"/>
		</connections>
	</instance>

	<instance id="oracle10g" dbase="oracle">
		<listeners>
			<listener port="9001" socket="/tmp/oracletest.socket"/>
		</listeners>
		<auths>
			<auth module="sqlrclient_userlist">
				<user user="testuser" password="testpassword"/>
			</auth>
		</auths>
		<connections>
                        <connection string="oracle_sid=(DESCRIPTION = (ADDRESS = (PROTOCOL = TCP)(HOST = oracle10g)(PORT = 1521)) (CONNECT_DATA = (SERVER = DEDICATED) (SERVICE_NAME = ora1)));user=@HOSTNAME@;password=testpassword"/>
		</connections>
	</instance>

</instances>
