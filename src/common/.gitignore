# defines.h is generated by configure from defines.h.in (it is in
# configure.in's MAKELIST) and must not be tracked (#9678).  it holds the
# branding, paths and version this checkout was configured with, so a
# tracked copy is just one machine's configure run.  defines.h.in is hand
# maintained and stays tracked
/defines.h
